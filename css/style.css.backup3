/* 全局样式 */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    line-height: 1.6;
    color: #333;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    min-height: 100vh;
}

/* 头部样式 */
header {
    background: rgba(255, 255, 255, 0.95);
    padding: 2rem;
    text-align: center;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

header h1 {
    font-size: 2.5rem;
    margin-bottom: 0.5rem;
    color: #2c3e50;
}

header p {
    font-size: 1.2rem;
    color: #7f8c8d;
    margin-bottom: 1rem;
}

nav {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 1rem;
}

nav a {
    text-decoration: none;
    color: #3498db;
    font-weight: 600;
    padding: 0.5rem 1rem;
    border-radius: 5px;
    transition: all 0.3s ease;
}

nav a:hover {
    background: #3498db;
    color: white;
}

/* 主内容区 */
main {
    max-width: 1200px;
    margin: 2rem auto;
    padding: 0 2rem;
}

section {
    background: white;
    margin: 2rem 0;
    padding: 2rem;
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
}

h2 {
    color: #2c3e50;
    margin-bottom: 1.5rem;
    font-size: 1.8rem;
    border-bottom: 3px solid #3498db;
    padding-bottom: 0.5rem;
}

/* 概念卡片 */
.concept {
    background: #f8f9fa;
    padding: 1.5rem;
    border-radius: 8px;
    border-left: 4px solid #3498db;
}

.concept ul {
    list-style: none;
    margin-top: 1rem;
}

.concept li {
    padding: 0.5rem 0;
    font-size: 1.1rem;
}

/* 机制网格 */
.mechanism-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 1.5rem;
    margin-top: 1.5rem;
}

.mechanism-card {
    background: #f8f9fa;
    padding: 1.5rem;
    border-radius: 8px;
    border-left: 4px solid #e74c3c;
    transition: transform 0.3s ease;
}

.mechanism-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
}

.mechanism-card h3 {
    color: #2c3e50;
    margin-bottom: 0.5rem;
    font-size: 1.3rem;
}

.mechanism-card .tagline {
    font-style: italic;
    color: #7f8c8d;
    margin-bottom: 1rem;
}

.mechanism-card p {
    color: #555;
    font-size: 0.95rem;
}

/* ===== 代码块容器 — VS Code 风格 ===== */

.code-analysis {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
    gap: 1.5rem;
}

.code-block {
    background: #1e1e1e;
    border-radius: 8px;
    overflow: hidden;
    box-shadow: 0 4px 20px rgba(0,0,0,0.3), 0 0 0 1px rgba(255,255,255,0.05);
    margin: 1.5rem 0;
}

.code-block > h3 {
    background: #2d2d2d;
    margin: 0;
    padding: 0.7rem 1rem;
    font-size: 0.85rem;
    color: #cccccc;
    font-weight: 500;
    border-bottom: 1px solid #404040;
    display: flex;
    align-items: center;
    gap: 0.5rem;
}

.code-block > h3::before {
    content: '';
    display: inline-block;
    width: 12px;
    height: 12px;
    border-radius: 50%;
    background: #ff5f57;
    box-shadow: 20px 0 0 #febc2e, 40px 0 0 #28c840;
}

.code-block > pre { margin: 0; background: #1e1e1e; }

/* ===== 代码切换标签 ===== */
.code-tabs {
    display: flex;
    gap: 0;
    margin-bottom: 0;
    background: #252526;
    border-radius: 8px 8px 0 0;
    overflow: hidden;
    border: 1px solid #3c3c3c;
    border-bottom: none;
}

.code-tab {
    padding: 0.55rem 1.1rem;
    background: transparent;
    border: none;
    border-right: 1px solid #3c3c3c;
    cursor: pointer;
    font-size: 0.82rem;
    font-weight: 500;
    color: #858585;
    transition: all 0.15s ease;
    position: relative;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.code-tab:hover { background: #2d2d2d; color: #cccccc; }
.code-tab.active { background: #1e1e1e; color: #ffffff; }
.code-tab.active::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 2px;
    background: #007acc;
}

/* ===== 代码内容区域 ===== */
.code-content {
    position: relative;
    background: #1e1e1e;
    border-radius: 0 0 8px 8px;
    border: 1px solid #3c3c3c;
    border-top: none;
    overflow: hidden;
}

.code-panel { display: none; margin: 0; padding: 0; }
.code-panel.active { display: block; }

/* ===== 增强代码块 — 行号 + 语法高亮 ===== */

.code-header {
    background: #252526;
    padding: 0.45rem 0.8rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #3c3c3c;
    min-height: 32px;
}

.code-lang {
    font-size: 0.72rem;
    font-weight: 600;
    color: #858585;
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.code-actions { display: flex; gap: 0.3rem; }

.code-action-btn {
    background: transparent;
    border: 1px solid transparent;
    border-radius: 4px;
    padding: 0.25rem 0.5rem;
    cursor: pointer;
    font-size: 0.72rem;
    color: #858585;
    transition: all 0.15s ease;
    display: flex;
    align-items: center;
    gap: 0.3rem;
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
}

.code-action-btn:hover { background: #3c3c3c; color: #cccccc; border-color: #555; }
.code-action-btn.copied { color: #4ec9b0; border-color: #4ec9b0; }

.code-lines {
    overflow-x: auto;
    overflow-y: auto;
    max-height: 600px;
    scrollbar-width: thin;
    scrollbar-color: #424242 #1e1e1e;
}

.code-lines::-webkit-scrollbar { width: 8px; height: 8px; }
.code-lines::-webkit-scrollbar-track { background: #1e1e1e; }
.code-lines::-webkit-scrollbar-thumb { background: #424242; border-radius: 4px; }
.code-lines::-webkit-scrollbar-thumb:hover { background: #555; }

.code-line {
    display: flex;
    font-family: 'JetBrains Mono', 'Fira Code', 'SF Mono', 'Cascadia Code', Consolas, 'Courier New', monospace;
    font-size: 13.5px;
    line-height: 1.65;
    min-height: 1.65em;
    color: #d4d4d4;
    -webkit-font-smoothing: antialiased;
}

.code-line:hover { background: rgba(255,255,255,0.03); }

.line-number {
    color: #6e7681;
    background: #1e1e1e;
    padding: 0 0.9rem 0 1rem;
    text-align: right;
    user-select: none;
    min-width: 2.8rem;
    border-right: 1px solid #3c3c3c;
    flex-shrink: 0;
    font-variant-numeric: tabular-nums;
}

.line-content {
    padding: 0 1rem 0 0.8rem;
    flex: 1;
    white-space: pre;
    tab-size: 4;
}

/* ===== 语法高亮 — VS Code Dark+ ===== */
.hl-keyword     { color: #569cd6; }
.hl-function    { color: #dcdcaa; }
.hl-string      { color: #ce9178; }
.hl-comment     { color: #6a9955; font-style: italic; }
.hl-number      { color: #b5cea8; }
.hl-operator    { color: #d4d4d4; }
.hl-class       { color: #4ec9b0; }
.hl-type        { color: #4ec9b0; }
.hl-decorator   { color: #dcdcaa; }
.hl-builtin     { color: #4fc1ff; }
.hl-variable    { color: #9cdcfe; }
.hl-property    { color: #9cdcfe; }
.hl-constant    { color: #4fc1ff; }
.hl-punctuation { color: #d4d4d4; }

/* 全屏模式 */
.code-block.fullscreen,
.code-block-enhanced.fullscreen {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    z-index: 10000;
    border-radius: 0;
    margin: 0;
    box-shadow: none;
}

.code-block.fullscreen .code-lines,
.code-block-enhanced.fullscreen .code-lines {
    max-height: none;
    height: calc(100vh - 32px);
}

.fullscreen-overlay {
    position: fixed;
    top: 0; left: 0; right: 0; bottom: 0;
    background: rgba(0,0,0,0.7);
    z-index: 9999;
    backdrop-filter: blur(4px);
}

/* ===== 旧版兼容 ===== */
.code-block code {
    color: #d4d4d4;
    font-family: 'JetBrains Mono', 'Fira Code', 'SF Mono', Consolas, monospace;
    font-size: 13.5px;
    line-height: 1.65;
    background: #1e1e1e;
    display: block;
    padding: 1rem;
    overflow-x: auto;
    white-space: pre;
}

pre {
    background: #1e1e1e;
    border-radius: 6px;
    overflow: hidden;
    margin: 1rem 0;
}

pre code {
    display: block;
    padding: 1rem 1.2rem;
    font-family: 'JetBrains Mono', 'Fira Code', 'SF Mono', Consolas, monospace;
    font-size: 13.5px;
    line-height: 1.65;
    color: #d4d4d4;
    background: #1e1e1e;
    overflow-x: auto;
    white-space: pre;
    -webkit-font-smoothing: antialiased;
}

.mermaid {
    background: white;
    padding: 1.5rem;
    border-radius: 8px;
    margin-top: 1rem;
}

footer {
    background: rgba(255,255,255,0.95);
    padding: 1.5rem;
    text-align: center;
    margin-top: 3rem;
    box-shadow: 0 -4px 6px rgba(0,0,0,0.1);
}

footer a { color: #3498db; text-decoration: none; }
footer a:hover { text-decoration: underline; }

/* ===== 响应式 ===== */
@media (max-width: 768px) {
    header h1 { font-size: 2rem; }
    nav { flex-direction: column; gap: 1rem; }
    .mechanism-grid { grid-template-columns: 1fr; }
    .code-analysis { grid-template-columns: 1fr; }
    .code-line { font-size: 12.5px; }
    .line-number { padding: 0 0.5rem; min-width: 2rem; font-size: 11px; }
    .line-content { padding: 0 0.6rem; }
    .code-tab { padding: 0.45rem 0.7rem; font-size: 0.78rem; }
    .code-header { padding: 0.35rem 0.6rem; }
    .code-action-btn { padding: 0.2rem 0.4rem; font-size: 0.68rem; }
    .code-block { border-radius: 6px; margin: 1rem 0; }
    .code-tabs { border-radius: 6px 6px 0 0; }
    .code-content { border-radius: 0 0 6px 6px; }
}

@media (max-width: 480px) {
    .code-line { font-size: 11.5px; }
    .line-number { padding: 0 0.4rem; min-width: 1.6rem; font-size: 10px; }
    .line-content { padding: 0 0.4rem; }
    .code-tab { padding: 0.35rem 0.5rem; font-size: 0.72rem; }
    .code-lines { max-height: 400px; }
}


/* 全屏模式样式 */
.code-fullscreen {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: #1e1e1e;
    z-index: 1000;
    padding: 2rem;
    overflow: auto;
}

.code-fullscreen code {
    font-size: 16px !important;
    padding: 2rem !important;
}

/* 动画效果 */
@keyframes fadeIn {
    from { opacity: 0; transform: translateY(10px); }
    to { opacity: 1; transform: translateY(0); }
}

.code-content {
    animation: fadeIn 0.3s ease-out;
}

/* 选中代码样式 */
::selection {
    background: #264f78;
    color: white;
}

::-moz-selection {
    background: #264f78;
    color: white;
}
